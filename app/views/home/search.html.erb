<br>
<div class="form-group row">
    <div class="col-sm-3 col-md-3">
      <!-- FORM SEACH -->
      <div class="panel panel-default">
        <div class="panel-body">
          <p>
            I need...
          </p>
          <%= form_tag 'search' do %>
            <%= text_field_tag :name, session[:search_name], class: "form-control", placeholder:"Tool Name" %><br>
            <%= select_tag :category_id, options_from_collection_for_select(@categories, :id, :name, session[:search_category]), include_blank: 'Any category', class:"form-control" %><br>
            <%= submit_tag "Search", class:"btn btn-default" %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="col-sm-9 col-md-9">
      <!-- RESULTS -->
      <div class="row">
        <% @result.each do |offer| %>
          <div class="col-sm-3 col-md-3">
            <div class="thumbnail">
            <td><%= image_tag offer.pictures.first.image.thumbnail.url %></td>
              <h4><%= link_to offer.name, new_rent_path(offer_id: offer.id) %></h4>
              <%= offer.category.name %>
              <%= "€ #{offer.price}" %>
              </a>
            </div>
          </div>
        <% end %>
      </div>

    </div>
  </div>
